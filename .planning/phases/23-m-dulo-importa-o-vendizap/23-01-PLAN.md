# Plano 23-01: Módulo de Importação Vendizap (Backend & Frontend)

Este plano descreve a implementação técnica do módulo de importação de dados do Vendizap, cobrindo as atualizações de banco de dados, o módulo isolado de importação no backend seguindo a Clean Architecture e a construção da interface do painel administrativo.

---

## 🎯 Objetivos do Desenvolvimento

1. **Modelagem de Dados**: Atualizar o `schema.prisma` adicionando o campo `externalId` nos modelos `Product` e `Order` para identificar os registros vindos do Vendizap e suportar operações de UPSERT.
2. **Arquitetura Limpa Modular (Backend)**:
   - Criar o módulo `imports` (ou `vendizap-import`).
   - Implementar serviços de infraestrutura para conectar na API externa e baixar imagens.
   - Criar casos de uso específicos para sincronizar Categorias, Produtos (e suas Variações) e Pedidos.
3. **Frontend (Admin)**:
   - Criar a página `ImportsPage` no módulo Admin.
   - Adicionar botões para disparar os processos de importação.
   - Exibir estado de carregamento e feedback visual ao usuário.

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files (`prisma/schema.prisma`). The planner MUST include a `[BLOCKING]` task that runs the database schema push command AFTER all schema file modifications are complete but BEFORE verification.

- ORM detected: Prisma
- Push command: `npx prisma db push`
- Non-TTY workaround: `npx prisma db push --accept-data-loss` (if destructive)

This task is mandatory — the phase CANNOT pass verification without it. Build and type checks will pass without the push (types come from config, not the live database), creating a false-positive verification state.
</schema_push_requirement>

---

## 🛠️ Detalhamento Técnico das Mudanças

### Onda 1: Esquema de Banco e Migração (Prisma)

#### 1. Atualização do Schema Prisma
- **Arquivo**: `prisma/schema.prisma`
- **Modificações**:
  - Adicionar o campo `externalId` ao model `Product`:
    ```prisma
    externalId String? @unique
    ```
  - Adicionar o campo `externalId` ao model `Order`:
    ```prisma
    externalId String? @unique
    ```
- **Ação [BLOCKING]**: Executar `npx prisma db push` (ou `npx prisma db push --accept-data-loss`) para aplicar as alterações de esquema no banco local.

---

### Onda 2: Infraestrutura de Integração e Storage (`src/modules/imports/infrastructure`)

#### 1. Integração com a API do Vendizap
- **Arquivo**: `src/modules/imports/infrastructure/services/vendizap.service.ts`
- **Responsabilidade**: Fazer requisições HTTP (`axios` ou `fetch`) para a API do Vendizap (endpoints de categorias, produtos, variações, pedidos) injetando os cabeçalhos `X-Auth-Id` e `X-Auth-Secret`.

#### 2. Serviço de Download e Upload de Imagens
- **Arquivo**: `src/modules/imports/infrastructure/services/image-migration.service.ts`
- **Responsabilidade**: Dado uma URL de imagem do Vendizap, fazer o download (buffer) e enviar para o provedor de storage atual do sistema, retornando a nova URL (CDN própria).

---

### Onda 3: Casos de Uso (Business Logic)

#### 1. Sincronização de Categorias
- **Arquivo**: `src/modules/imports/domain/use-cases/import-categories.use-case.ts`
- **Lógica**: Buscar categorias da API, migrar imagem se existir, e usar o `ICategoriesRepository` (que precisará de um método `upsertByExternalId`) para salvar.

#### 2. Sincronização de Variações e Produtos
- **Arquivos**:
  - `src/modules/imports/domain/use-cases/import-variations.use-case.ts`
  - `src/modules/imports/domain/use-cases/import-products.use-case.ts`
- **Lógica**: Migrar variações, depois buscar produtos. Mapear variações para produtos. Fazer download de todas as imagens, cadastrar os produtos no `IProductsRepository` verificando o `externalId`.

#### 3. Sincronização de Pedidos
- **Arquivo**: `src/modules/imports/domain/use-cases/import-orders.use-case.ts`
- **Lógica**: Associar com Clientes (por telefone), inserir o Pedido com `externalId` e atrelar os itens (buscando os produtos locais). Decidir sobre o desconto de estoque.

---

### Onda 4: Controladores API e Módulo NestJS

- **Arquivo**: `src/modules/imports/infrastructure/controllers/vendizap-imports.controller.ts`
- **Endpoints**:
  - `POST /imports/vendizap/categories`
  - `POST /imports/vendizap/variations`
  - `POST /imports/vendizap/products`
  - `POST /imports/vendizap/orders`
- **Arquivo**: `src/modules/imports/imports.module.ts` (registrar todos os providers e controllers).

---

### Onda 5: Painel Administrativo (Frontend)

#### 1. Integração de Serviço Frontend
- **Arquivo**: `ecommerce-admin-front/src/services/imports.service.ts`
- **Responsabilidade**: Chamar os endpoints HTTP criados no backend.

#### 2. Página de Importações
- **Arquivo**: `ecommerce-admin-front/src/pages/ImportsPage.tsx`
- **Conteúdo**: Tela com seções (Categorias, Produtos, Pedidos) e botões "Sincronizar Agora". Feedback visual de loading (spinners) para operações que demoram.
- **Roteamento**: Adicionar a rota em `ecommerce-admin-front/src/App.tsx` e o link no menu lateral (`Sidebar`).

---

## 🧪 Plano de Validação Automatizada e Manual
1. **Verificação do Schema Push**: Garantir que `npx prisma db push` execute com sucesso (Automático).
2. **Testes Unitários**: Testes para os casos de uso de importação com serviços de mock para a API do Vendizap (Automático).
3. **Teste de UI**: Verificar se a página de importação aparece corretamente (Manual).
