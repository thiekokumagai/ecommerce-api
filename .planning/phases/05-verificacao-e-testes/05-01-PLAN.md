# Phase 5 Plan: 05-01 - Execução geral de testes unitários, E2E e validação Swagger

## Objective
Garantir que a suíte de testes Jest (unitários e E2E) execute com 100% de sucesso e que a documentação Swagger permaneça acessível e consistente após a refatoração Clean Architecture.

## Tasks

### 1. Corrigir configuração Jest
- [ ] Ajustar `package.json` e `test/jest-e2e.json` para resolver `ts-jest` com `rootDir` na raiz do projeto.

### 2. Testes unitários
- [ ] Executar `npm test` e garantir que specs existentes passem.

### 3. Testes E2E (Supertest)
- [ ] Criar helper de bootstrap espelhando `main.ts` (prefix `api`, pipes, Swagger).
- [ ] Cobrir login, endpoints protegidos e documentação Swagger.

### 4. Validação final
- [ ] `npm run build` sem erros.
- [ ] Registrar resultados em `05-01-SUMMARY.md`.
